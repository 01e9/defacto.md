{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col">
                {% include 'admin/component/breadcrumb.html.twig' with {
                    current: {title: 'title.settings'|trans}
                } only %}
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h3>{{ 'title.settings'|trans }}</h3>
                {% include 'admin/component/list/settings.html.twig' with {
                    settings: settings
                } only %}
            </div>
        </div>
    </div>
{% endblock %}