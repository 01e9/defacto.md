{#- @var \DateTime blogLastModified -#}

<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <sitemap>
        <loc>{{ url('sitemap_blog') }}</loc>
        {% if blogLastModified %}
            <lastmod>{{ blogLastModified|date(constant('\DateTimeInterface::W3C')) }}</lastmod>
        {% endif %}
    </sitemap>
    <sitemap>
        <loc>{{ url('sitemap_politicians') }}</loc>
    </sitemap>
</sitemapindex>