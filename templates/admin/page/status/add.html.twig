{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        {% include 'admin/component/breadcrumb.html.twig' with {
            items: [
                {title: 'title.settings'|trans, link: path('admin_settings')}
            ],
            current: {title: 'title.status'|trans}
        } only %}

        {% include 'admin/component/form/status.html.twig' with {form: form} only %}
    </div>
{% endblock %}