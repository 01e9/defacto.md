{{ form_start(form) }}

<div class="row">
    <div class="col">
        {{ form_row(form.value) }}
    </div>
</div>
<div class="row">
    <div class="col">
        <button type="submit" class="btn btn-primary">{{ 'action.edit_setting'|trans }}</button>
    </div>
</div>

{{ form_rest(form) }}

{{ form_end(form) }}