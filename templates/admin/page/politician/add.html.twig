{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        {% include 'admin/component/breadcrumb.html.twig' with {
            items: [
                {title: 'title.politicians'|trans, link: path('admin_politicians')}
            ],
            current: {title: 'title.politician_add'|trans}
        } only %}

        {% include 'admin/component/form/politician.html.twig' with {form: form} only %}
    </div>
{% endblock %}