<div class="list-group list-group-flush">
    {% for problem in problems %}
        <a href="{{ path('admin_problem_edit', {id: problem.id}) }}" class="list-group-item list-group-item-action">
            {{ problem.name }}
        </a>
    {% endfor %}
</div>