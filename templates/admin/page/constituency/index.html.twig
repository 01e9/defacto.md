{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col">
                {% include 'admin/component/breadcrumb.html.twig' with {
                    current: {title: 'title.constituencies'|trans}
                } only %}
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h3>
                    {{ 'title.constituencies'|trans }}
                    <sup>
                        <a href="{{ path('admin_constituency_add') }}" class="badge badge-light text-muted"
                        >{{ 'action.add'|trans }}</a>
                    </sup>
                </h3>
                {% include 'admin/component/list/constituencies.html.twig' with {
                    constituencies: constituencies
                } only %}
            </div>
        </div>
    </div>
{% endblock %}