<div class="list-group list-group-flush">
    {% for constituency in constituencies %}
        <a href="{{ path('admin_constituency_edit', {id: constituency.id}) }}" class="list-group-item list-group-item-action">
            {{ constituency.name }}
        </a>
    {% endfor %}
</div>