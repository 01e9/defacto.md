{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col">
                {% include 'admin/component/breadcrumb.html.twig' with {
                    current: {title: 'title.problems'|trans}
                } only %}
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h3>
                    {{ 'title.problems'|trans }}
                    <sup>
                        <a href="{{ path('admin_problem_add') }}" class="badge badge-light text-muted"
                        >{{ 'action.add'|trans }}</a>
                    </sup>
                </h3>
                {% include 'admin/component/list/problems.html.twig' with {
                    problems: problems
                } only %}
            </div>
        </div>
    </div>
{% endblock %}