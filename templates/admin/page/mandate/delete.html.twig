{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        {% include 'admin/component/breadcrumb.html.twig' with {
            items: [
                {title: 'title.mandates'|trans, link: path('admin_mandates')}
            ],
            current: {title: 'title.mandate_delete'|trans},
            add: {link: path('admin_mandate_add'), text: 'action.add_mandate'|trans}
        } only %}

        {{ form_start(form) }}
        <div class="row">
            <div class="col">
                <h3>{{ mandate.choiceName }}</h3>
                {{ form_errors(form) }}
                <p>
                    <button type="submit" class="btn btn-danger">
                        <span class="fa fa-trash-alt"></span>
                        {{ 'action.delete_mandate'|trans }}
                    </button>
                </p>
            </div>
        </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}