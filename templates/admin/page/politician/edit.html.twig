{# @var \Symfony\Component\Form\FormInterface form #}
{# @var \App\Entity\Politician politician #}

{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="container">
        {% include 'admin/component/breadcrumb.html.twig' with {
            items: [
                {title: 'title.politicians'|trans, link: path('admin_politicians')}
            ],
            current: {title: 'title.politician_edit'|trans},
            add: {link: path('admin_politician_add'), text: 'action.add_politician'|trans},
            frontendLink: path("politician", {slug: politician.slug})
        } only %}

        {% include 'admin/component/form/politician.html.twig' with {form: form, politician: politician} only %}
    </div>
{% endblock %}